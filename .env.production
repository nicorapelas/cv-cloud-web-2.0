# Disable treating warnings as errors during production builds
# This allows the build to complete even with warnings
# TODO: Fix all linting issues and critical dependency warnings, then remove these flags
CI=false
DISABLE_ESLINT_PLUGIN=true
